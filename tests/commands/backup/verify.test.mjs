import { jest } from '@jest/globals'; import { runBackupVerify } from '../../../src/commands/backup/verify.mjs'; test('verifies a backup', async () => { const verifyBackup = jest.fn(async () => ({ ok: true })); await runBackupVerify({ client: { lease: async () => ({}) }, verifyBackup, emit: jest.fn(), database: 'db', identity: 'id', root: 'root' }); expect(verifyBackup).toHaveBeenCalledWith(expect.objectContaining({ backupPath: 'root' })); });
