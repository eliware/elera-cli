import { runApplicationCreate, runApplicationStatus, runAppAdminCreate } from '../../src/commands/app.mjs';
import { jest } from '@jest/globals';
test('runs application administration commands with friendly-name status lookup', async () => { const emit = jest.fn(); const applicationStatus = jest.fn(async (id) => ({ id })); const client = { createApplication: async (name) => ({ name }), applications: async () => ({ data: [{ id: '1', name: 'Demo' }] }), applicationStatus, createAppAdminToken: async (application, tokenName) => ({ application, tokenName }) }; await runApplicationCreate({ client, emit, name: 'Demo' }); await runApplicationStatus({ client, emit, applicationId: 'Demo' }); await runAppAdminCreate({ client, emit, application: 'Demo', tokenName: 'owner' }); expect(applicationStatus).toHaveBeenCalledWith('1'); expect(emit).toHaveBeenCalledTimes(3); });
test('accepts an unwrapped application list and stable application id', async () => { const applicationStatus = jest.fn(async (id) => ({ id })); const client = { applications: async () => [{ id: '1', name: 'Demo' }], applicationStatus }; await expect(runApplicationStatus({ client, emit: jest.fn(), applicationId: '1' })).resolves.toBe(0); expect(applicationStatus).toHaveBeenCalledWith('1'); });
test('accepts an object-wrapped application list', async () => { const applicationStatus = jest.fn(async (id) => ({ id })); const client = { applications: async () => ({ applications: [{ id: '2', name: 'Other' }] }), applicationStatus }; await expect(runApplicationStatus({ client, emit: jest.fn(), applicationId: 'Other' })).resolves.toBe(0); expect(applicationStatus).toHaveBeenCalledWith('2'); });
test('rejects an application response without records', async () => { await expect(runApplicationStatus({ client: { applications: async () => ({}) }, emit: jest.fn(), applicationId: 'Missing' })).rejects.toThrow('application not found'); });
