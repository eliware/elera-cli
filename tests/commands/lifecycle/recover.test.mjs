import { jest } from '@jest/globals'; import { runLifecycleRecover } from '../../../src/commands/lifecycle/recover.mjs'; test('runs lifecycle recovery', async () => { const recover = jest.fn(async () => ({ ok: true })); const lifecycle = { recover, classify: () => 0 }; await runLifecycleRecover({ lifecycle, emit: jest.fn(), target: 'elera-1' }); expect(recover).toHaveBeenCalledWith('elera-1'); });
