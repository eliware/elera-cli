export async function runRestoreAccountsVerify({ client, emit, value }) { const result = await client.restoreAccountsVerify(JSON.parse(value ?? '[]')); emit(result); return result.ok === false ? 1 : 0; }
