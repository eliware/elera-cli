export async function runSqlSmoke({ client, createLive, createDb, emit, config }) { const bundle = await client.lease(config.database, config.identity); const live = await createLive({ bundle, endpoint: config.endpoint, token: config.token, application: config.database, fetchBundle: async () => client.routingBundle(config.identity), createDb }); try { const [rows] = await live.db.query('SELECT 1 AS healthy'); const ok = rows[0]?.healthy === 1; emit({ ok }); return ok ? 0 : 1; } finally { await live.close(); } }
